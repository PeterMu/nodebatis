<?xml version='1.0' encoding='utf-8'?>
<mapper namespace='a'>
    <select id='selectAll'>
        select * from a where a.id = :id and a.name like :name 
        <if test='age>0 and sex == "M"'>
            and a.age = :age
        </if>
        and a.a = a
    </select>
</mapper>
