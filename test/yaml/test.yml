namespace: 'test'

attrs: name, age

query:
    - select {{ test.attrs }} from test where
    - if:
        test: :age > 18
        sql: and age = :age
    - if:
        test: :name
        sql: and name = :name
